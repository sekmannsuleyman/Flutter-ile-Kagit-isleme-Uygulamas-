import 'package:flutter/material.dart';

class UploadDirectoryPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Upload Directory'),
      ),
      body: Center(
        child: Text('Upload Directory Page Content'),
      ),
    );
  }
}
